/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.cohere.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class GenerateStreamedResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private GenerateStreamedResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static GenerateStreamedResponse textGeneration(GenerateStreamText value) {
        return new GenerateStreamedResponse(new TextGenerationValue(value));
    }

    public static GenerateStreamedResponse streamEnd(GenerateStreamEnd value) {
        return new GenerateStreamedResponse(new StreamEndValue(value));
    }

    public static GenerateStreamedResponse streamError(GenerateStreamError value) {
        return new GenerateStreamedResponse(new StreamErrorValue(value));
    }

    public boolean isTextGeneration() {
        return value instanceof TextGenerationValue;
    }

    public boolean isStreamEnd() {
        return value instanceof StreamEndValue;
    }

    public boolean isStreamError() {
        return value instanceof StreamErrorValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<GenerateStreamText> getTextGeneration() {
        if (isTextGeneration()) {
            return Optional.of(((TextGenerationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<GenerateStreamEnd> getStreamEnd() {
        if (isStreamEnd()) {
            return Optional.of(((StreamEndValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<GenerateStreamError> getStreamError() {
        if (isStreamError()) {
            return Optional.of(((StreamErrorValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitTextGeneration(GenerateStreamText textGeneration);

        T visitStreamEnd(GenerateStreamEnd streamEnd);

        T visitStreamError(GenerateStreamError streamError);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "event_type",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(TextGenerationValue.class),
        @JsonSubTypes.Type(StreamEndValue.class),
        @JsonSubTypes.Type(StreamErrorValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("text-generation")
    @JsonIgnoreProperties("event_type")
    private static final class TextGenerationValue implements Value {
        @JsonUnwrapped
        private GenerateStreamText value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TextGenerationValue() {}

        private TextGenerationValue(GenerateStreamText value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTextGeneration(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TextGenerationValue && equalTo((TextGenerationValue) other);
        }

        private boolean equalTo(TextGenerationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "GenerateStreamedResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("stream-end")
    @JsonIgnoreProperties("event_type")
    private static final class StreamEndValue implements Value {
        @JsonUnwrapped
        private GenerateStreamEnd value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StreamEndValue() {}

        private StreamEndValue(GenerateStreamEnd value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitStreamEnd(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StreamEndValue && equalTo((StreamEndValue) other);
        }

        private boolean equalTo(StreamEndValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "GenerateStreamedResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("stream-error")
    @JsonIgnoreProperties("event_type")
    private static final class StreamErrorValue implements Value {
        @JsonUnwrapped
        private GenerateStreamError value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StreamErrorValue() {}

        private StreamErrorValue(GenerateStreamError value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitStreamError(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StreamErrorValue && equalTo((StreamErrorValue) other);
        }

        private boolean equalTo(StreamErrorValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "GenerateStreamedResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("event_type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "GenerateStreamedResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
