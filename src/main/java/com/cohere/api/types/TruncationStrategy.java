/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.cohere.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class TruncationStrategy {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private TruncationStrategy(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static TruncationStrategy auto(TruncationStrategyAutoPreserveOrder value) {
        return new TruncationStrategy(new AutoValue(value));
    }

    public static TruncationStrategy none(TruncationStrategyNone value) {
        return new TruncationStrategy(new NoneValue(value));
    }

    public boolean isAuto() {
        return value instanceof AutoValue;
    }

    public boolean isNone() {
        return value instanceof NoneValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TruncationStrategyAutoPreserveOrder> getAuto() {
        if (isAuto()) {
            return Optional.of(((AutoValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TruncationStrategyNone> getNone() {
        if (isNone()) {
            return Optional.of(((NoneValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAuto(TruncationStrategyAutoPreserveOrder auto);

        T visitNone(TruncationStrategyNone none);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(AutoValue.class), @JsonSubTypes.Type(NoneValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("auto")
    private static final class AutoValue implements Value {
        @JsonUnwrapped
        private TruncationStrategyAutoPreserveOrder value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AutoValue() {}

        private AutoValue(TruncationStrategyAutoPreserveOrder value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAuto(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AutoValue && equalTo((AutoValue) other);
        }

        private boolean equalTo(AutoValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TruncationStrategy{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("none")
    private static final class NoneValue implements Value {
        @JsonUnwrapped
        private TruncationStrategyNone value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private NoneValue() {}

        private NoneValue(TruncationStrategyNone value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitNone(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof NoneValue && equalTo((NoneValue) other);
        }

        private boolean equalTo(NoneValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TruncationStrategy{" + "value: " + value + "}";
        }
    }

    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TruncationStrategy{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
